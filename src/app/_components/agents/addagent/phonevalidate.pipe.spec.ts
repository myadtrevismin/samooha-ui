import { PhonevalidatePipe } from './phonevalidate.pipe';

describe('PhonevalidatePipe', () => {
  it('create an instance', () => {
    const pipe = new PhonevalidatePipe();
    expect(pipe).toBeTruthy();
  });
});
